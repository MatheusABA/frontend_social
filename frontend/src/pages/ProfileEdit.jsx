import React from 'react'
import { Stack, HStack, Flex, Image, Box, Text, Button } from '@chakra-ui/react'

const Perfil = (props) => {
    props.setIsUpload = false
    return (
        <div>Perfil</div>
    )
}

export default Perfil